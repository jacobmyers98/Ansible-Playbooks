- hosts: all
  become: true
  tasks:
  - name: Install all packages for the host
    shell:
      cmd: "echo $HOME"
    register: VAR

  - name: Show result content only
    debug:
    shell: mkdir {{ VAR }}/test
